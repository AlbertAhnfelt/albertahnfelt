---
import Moon from './Moon.svelte'
import { clickyMode } from '../config'

const { variant = 'bottom' } = Astro.props
---
<div class:list={['stack', variant]}>
  <div class="moon">
    <Moon clicky={clickyMode} client:load transition:persist="moon"></Moon>
  </div>
  <div class="title">
    <a href="/">albertahnfelt.com</a>
  </div>
</div>

<style>
  .stack.bottom,
  .stack.center {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 3rem 0 2rem;
  }

  /* Sticky footer, for pages whose content grows downward. Stays in flow, so
     it can never overlap. `margin-top: auto` eats the leftover space in a
     flex-column parent — the moon rests at the bottom of a short page, and
     collapses to zero once the content is tall enough to push it down. The
     padding-top is the guaranteed minimum gap above it. */
  .stack.bottom-center {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-top: auto;
    padding: 6rem 0 2rem;
  }

  .stack.top {
    position: absolute;
    top: 1rem;
    right: 1rem;
  }

  .moon {
    position: absolute;
    left: 50%;
  }

  .stack.bottom .moon,
  .stack.center .moon,
  .stack.bottom-center .moon {
    position: relative;
    left: auto;
    width: 8rem;
    height: 8rem;
    margin-bottom: 0.5rem;
  }

  .stack.top .moon {
    top: 100%;
    margin-left: -4rem;
    margin-top: 0.5rem;
    width: 8rem;
    height: 8rem;
  }

  .title {
    font-size: 1.4rem;
    text-shadow: 0.12rem 0px 0px var(--black);
  }

  @media (min-width: 768px) {
    .stack.bottom {
      position: fixed;
      bottom: 1.5rem;
      right: 1.5rem;
      display: block;
      padding: 0;
    }

    .stack.top {
      top: 1.5rem;
      right: 1.5rem;
    }

    .stack.bottom .moon {
      position: absolute;
      left: 50%;
      bottom: 100%;
      width: 12rem;
      height: 12rem;
      margin-left: -6rem;
      margin-bottom: 0.5rem;
    }

    .stack.center .moon,
    .stack.bottom-center .moon {
      width: 12rem;
      height: 12rem;
    }

    .stack.bottom .title,
    .stack.center .title,
    .stack.bottom-center .title {
      font-size: 2rem;
    }

    .stack.top .title {
      font-size: 1.5rem;
    }
  }
</style>
